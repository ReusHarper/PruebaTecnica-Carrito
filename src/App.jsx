/* eslint-disable react-hooks/exhaustive-deps */
import { Outlet } from 'react-router-dom';

const App = () => {
    return (
        <App>
            <Outlet />
        </App>
    );
}

export default App;
